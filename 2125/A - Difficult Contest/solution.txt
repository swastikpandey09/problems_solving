#guyzzzzz we will use sliding window here because i think that "FFT" and "NTT" both are 3 letter words
#sliding_window_brick_length = 3
#if sliding window is fft or ntt then just continue one more time and swap it, do it in recurison until no. of fft or ntt becomes 0
'''
for i in range(int(input())):
    inputz = input()
    windows = [inputz[i:i+3] for i in range(len(inputz) - 3 + 1)]
    while "FFT" in windows and "NTT" in windows:
        windows = [inputz[i:i+3] for i in range(len(inputz) - 3 + 1)]
        j = 0
        for i in windows:
            if "FFT" == i:
                window[j] = "FTF"
                break
            if "NTT" == i:
                window[j] = "TNT"#minecraft enough
                break
            j+=1
'''
for i in range(int(input())):
    inputz = input()
    inputz = inputz.replace('FFT','FTF').replace('NTT','TNT')
    while "FFT" in inputz or "NTT" in inputz:
        inputz = inputz.replace('FFT','FTF').replace('NTT','TNT')
    #print('solutions')
    print(inputz)